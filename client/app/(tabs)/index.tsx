import { 
    View,
    SafeAreaView,
    Text,
} from "react-native";

import { Link } from "expo-router";

export default function HomePage(){
    return(
        <SafeAreaView>
            {
                
            }
        </SafeAreaView>
    )
}